
apiVersion: v1
kind: Service
metadata:
  name: {{ include "skblogsystem.fullname" . }}
  labels:
    {{- include "skblogsystem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "skblogsystem.selectorLabels" . | nindent 4 }}
